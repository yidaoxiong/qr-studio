/* =========================================================================
 * 启动引导
 * 必须放在所有模块之后执行：app-cloud.js 里有 const/let 声明，
 * 若在 app-ui.js 末尾直接调 init() 会撞上暂时性死区（TDZ）报错。
 * ========================================================================= */
init();
